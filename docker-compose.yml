version: '3.3'

services:
  bot:
    image: bot-mlm:latest
    ports:
      - target: 3000
        published: 3001
        mode: host
    deploy:
      replicas: 1
      restart_policy:
        condition: any
