version: '3'

services:
  bot:
    image: bot-mlm:v1.191
    build: .
    restart: always
    ports:
      - "3001:3000"
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
