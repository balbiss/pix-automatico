version: '3'

services:
  bot-final:
    container_name: bot-indicacao-v1254
    image: bot-indicacao:v1254
    build: .
    restart: always
    ports:
      - "3001:3000"
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
